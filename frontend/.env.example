# 前端环境变量(示例, 复制为 .env 使用)
# 默认走 Vite 代理(proxy -> http://localhost:8087), 留空即可
# 如需直连后端(绕过代理), 填 http://localhost:8087
VITE_API_BASE_URL=

# 「接口文档」页新窗口打开时使用的原始 Swagger UI 地址
# 留空则用同源相对路径 /swagger-ui/index.html(经 Vite 代理转发)
VITE_SWAGGER_URL=
